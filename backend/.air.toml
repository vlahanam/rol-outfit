root = "."
tmp_dir = "/tmp/air"

[build]
  cmd = "go build -o /tmp/air/server ./src/cmd"
  bin = "/tmp/air/server"
  include_ext = ["go"]
  exclude_dir = ["vendor", "testdata"]
  delay = 1000

[log]
  time = false

[color]
  main = "yellow"
  watcher = "cyan"
  build = "green"
  runner = "magenta"
